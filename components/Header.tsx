import React from 'react';
import Container from './Container';
import Logo from './Logo';

const header = () => {
  return (
    <header>
      <Container>
        <Logo/>
        {/* nav */}
        {/* nav admin */}
      </Container>
    </header>
  )
}

export default header